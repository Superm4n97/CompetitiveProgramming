#include<bits/stdc++.h>
#define ll long long int

using namespace std;

ll A,B,C,D , ans = 0;

int main()
{
      cin >> A >> B >> C >> D;
      for (int i=B ; i<=c ; i++){
            ll a = C+1 - i;
            ll b = D+1 - i;
            ll totalC = D-C+1;
            ll totalA = B-A+1;

            if (a>B) continue;

            if (b<A){
                  ans+=(totalA*totalC);
            }
            else if (a>=A && b<=B){

            }
      }


      return 0;
}
